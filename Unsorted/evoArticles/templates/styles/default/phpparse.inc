// you can exec any php stuff in here
// to use var in a template, use the $layout array
// to use custom value in a template, use the $custom array
// example: $custom[myname] = "James";
// now you can use $custom[myname] anywhere in the site's templates
