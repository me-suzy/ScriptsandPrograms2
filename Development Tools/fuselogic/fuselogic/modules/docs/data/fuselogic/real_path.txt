**API**

**Real_Path(string $module_name);**

**$module_name =** the name of FuseLogic module

almost the same with [[fuselogic:WebPath]]().

The diference is instead of returning the **"web path"**, it will returning **"real path"** of module.


----
//Credit List: Eko Budi Setiyo//